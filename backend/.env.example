# Copy to .env for local dev (NOT needed for Docker — compose sets these).
# For Docker, set these under `environment:` in docker-compose.yml instead.
DATABASE_URL="file:./dev.db"
PORT=4000
# Where uploaded product images are stored (defaults to ./uploads if unset):
# UPLOADS_DIR=./uploads
